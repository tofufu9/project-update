
@{ ViewBag.Title = "Index";
                Layout = "~/Views/Shared/_LayoutPage1.cshtml"; }
<!--
<div class="row justify-content-md-start" style="padding-top: 300px; padding-left: 850px">
    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                Welcome
            </div>
            <div class="card-body">
                @if (Session["FullName"] != null)
                {
                    <p>Name: @Session["FullName"].ToString()</p>
                    <p>Email: @Session["Email"].ToString()</p>
                }
            </div>
        </div>

    </div>
</div>
--> <div></div>
